import { seatArr } from "../Data";

const initialState = { soluong: 1, seatArr: seatArr };

export let MovieReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
